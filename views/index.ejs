using System;

namespace PrimeNumbers
{
    class Program
    {
            static void Main(string[] args)
            {
                int sayac = 0;
                int s1 = 1;
                Console.Write("Give Number : ");
                int s2 = Convert.ToInt32(Console.ReadLine());
                if (s1 == 1) 
                    s1++;
                Console.WriteLine();
                for (int sayi = s1; sayi <= s2; sayi++)
                {
                    int kontrol = 0;
                    for (int i = 2; i < sayi; i++)
                    {
                        if (sayi % i == 0) 
                        {
                            kontrol = 1;
                            break;
                        }
                    }
                    if (kontrol == 1)
                    {                       
                    }
                    else
                    {
                        Console.WriteLine("{0}", sayi);
                        sayac++;
                    }
                }
                Console.WriteLine("There is {0} prime numbers.", sayac);
                Console.ReadKey();
            }
        }
    }
